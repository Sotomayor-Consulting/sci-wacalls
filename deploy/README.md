# (movido)

Este directorio se reemplazó por [`deployments/`](../deployments), con una
variante por plataforma (Dokploy, Coolify) en vez de una guía genérica —
las dos difieren en cómo resuelven la red entre recursos del mismo servidor,
y mezclar las instrucciones produce errores confusos (404, timeouts).

Ver [`deployments/README.md`](../deployments/README.md).
